/* - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -  *//* MerchantApiConfig model                                                                        *//*                                                                                                *//* All database modifications go through the model; most querying is in the handlers.             *//* - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -  */'use strict';let MerchantApiConfig = module.exports = {};/** * Returns All MerchantApiConfig. * * @returns {Object} API Array. */MerchantApiConfig.getAll = function*(){  var sql = [    'select mac.id  as id, merchantid, mac.apiid as apiid, version, apis.url, daycallnum, daynumcalled, totalcallnum, totalnumcalled',    ' from merchantapiconfigs mac',    ' left join apis on mac.apiid = apis.id',    ' where apis.enabled = 1 order by merchantid'  ].join('');  let result = yield GLOBAL.db.query(sql);  return result[0];};